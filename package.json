{
  "name": "spine-skeleton-comparator",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "preview:cloudflare": "npm run build && wrangler dev"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.4",
    "wrangler": "^4.82.1"
  },
  "dependencies": {
    "@esotericsoftware/spine-core": "^4.2.109",
    "@esotericsoftware/spine-webgl": "^4.2.109"
  }
}
